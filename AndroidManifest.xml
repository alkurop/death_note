<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omar.deathnote"
    android:versionCode="12"
    android:versionName="1.0.2.free.prod" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

 
     <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER" />  

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true"
         />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.omar.deathnote.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.omar.deathnote.Note"
            android:label=" "
            android:launchMode="singleTop" >
        </activity>
        <activity
            android:name="com.omar.deathnote.pref.PrefActivity"
            android:label="Settings" >
        </activity>
        <activity
            android:name="com.omar.deathnote.pref.Contact"
            android:label="Contact" >
        </activity>
        <activity
            android:name="com.omar.deathnote.picview.SingleViewActivity"
            android:label=" " >
        </activity>

        <service
            android:name="com.omar.deathnote.audioplay.AudioPlayService"
            android:enabled="true" >
        </service>
    </application>

</manifest>